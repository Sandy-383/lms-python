# Curated for the deployed Flask backend (app.py) + its deep_learning/
# import chain. The separate Streamlit demo (deep_learning/app.py) is NOT
# deployed and is intentionally excluded from this environment; packages
# used ONLY by that demo were removed:
#   streamlit, altair, pydeck, uvicorn, starlette, anyio, h11, httptools,
#   websockets, python-multipart, watchdog, gitpython (+gitdb, +smmap),
#   cachetools, tenacity, toml, jsonschema (+jsonschema-specifications,
#   +referencing, +rpds-py), pyarrow
# Verified via each package's installed dist-info Requires-Dist metadata
# (see venv/Lib/site-packages/*.dist-info/METADATA) plus a runtime import
# check, not by name alone.
#
# jax, jaxlib, matplotlib, and narwhals LOOK like Streamlit/Altair-only
# packages but are kept: mediapipe's own metadata unconditionally requires
# jax/jaxlib/matplotlib (Requires-Dist, no environment marker), and
# scikit-learn 1.9.0 unconditionally requires narwhals. Removing them would
# break `pip install mediapipe` / `pip install scikit-learn` in this exact
# image, not just drop the Streamlit demo.
absl-py==2.5.0
attrs==26.1.0
blinker==1.9.0
certifi==2026.6.17
cffi==2.1.0
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
filelock==3.31.0
Flask==3.1.3
flask-cors==6.0.5
flatbuffers==25.12.19
fonttools==4.63.0
fsspec==2026.6.0
# gunicorn: production WSGI server -- app.py's own `python app.py` dev
# server (debug=True, Flask's built-in reloader) must never run in prod.
gunicorn==23.0.0
idna==3.18
itsdangerous==2.2.0
jax==0.11.0
jaxlib==0.11.0
Jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.5.0
MarkupSafe==3.0.3
matplotlib==3.11.1
mediapipe==0.10.14
ml_dtypes==0.5.4
mpmath==1.3.0
narwhals==2.24.0
networkx==3.6.1
numpy==2.5.1
opencv-contrib-python==5.0.0.93
opt_einsum==3.4.0
packaging==26.2
pandas==3.0.3
pillow==12.3.0
protobuf==4.25.9
pycparser==3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
requests==2.34.2
scikit-learn==1.9.0
scipy==1.18.0
setuptools==83.0.0
six==1.17.0
sounddevice==0.5.5
sympy==1.14.0
threadpoolctl==3.6.0
# torch: on Linux, plain `pip install torch` pulls ~2-3GB of CUDA/nvidia-*
# packages (see torch's own Requires-Dist, gated on platform_system==Linux)
# because the default PyPI wheel assumes a GPU host. The Dockerfile installs
# the CPU-only wheel explicitly (this VPS has no GPU) before this file is
# processed, so pip sees torch==2.13.0 already satisfied here and skips it.
torch==2.13.0
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
Werkzeug==3.1.8

# --- Added by the concurrent DB/auth workstream -- left untouched here ---
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
psycopg2-binary==2.9.12
python-dotenv==1.2.2
SQLAlchemy==2.0.51
alembic==1.18.5
greenlet==3.5.4
Mako==1.3.12
